class Solution(object):
    def findDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        mp={}
        for x in nums:
            if x in mp:
                return x
            else:
                mp[x]=1
